:orphan:

**Bug Fixes**

-  Model Registry APIs: sort models by name, description, and other attributes.

**Breaking Changes**

-  Model Registry APIs: use model ID (int) instead of name (str) as a lookup parameter in
   ``get_model`` and ``get_model_versions`` endpoints.
