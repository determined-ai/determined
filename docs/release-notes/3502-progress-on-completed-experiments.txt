:orphan:

**API Changes**

-  Calls to `/api/v1/experiments` or `/api/v1/experiments/:id` will allow the value
      of the `progress` field to be null instead of 0 in cases where the experiment is completed or
      otherwise not mid-process.
