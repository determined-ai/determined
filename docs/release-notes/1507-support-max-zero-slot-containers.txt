:orphan:

**Improvements**

-  Improve the experience of scheduling and scaling CPU tasks.
   Users can now configure ``max_cpu_containers_per_agent`` in the
   field ``provisioner`` of the master configuration. It defaults to no
   limit.
