:orphan:

###################
 Set Up Checklists
###################

Advanced On-Prem Setup Guide (one for each even though some might share same instructions)

Determined-agents - On-prem (one page, tabs for linux packages and docker containers) - det deploy
aws (its own page) - det deploy gcp (its own page)

Kubernetes - on-prem setup nodes - eks setup nodes - gke setup nodes - aks setup nodes - general k8s
setup — TLS *will have some shared steps among all options*

Slurm: - (one page)

.. toctree::
   :hidden:

   On Prem <advanced-on-prem>
   PostgreSQL <postgresql>
