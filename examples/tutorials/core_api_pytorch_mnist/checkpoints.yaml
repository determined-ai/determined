name: coreapi_mnist_tutorial_checkpoints
description: Save and load checkpoints as well as pause and resume experiments in the WebUI.
entrypoint:  python3 model_def_checkpoints.py 
max_restarts: 0
searcher:
   name: single
   max_length: 1
   metric: test_loss
