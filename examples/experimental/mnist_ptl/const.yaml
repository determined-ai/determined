description: mnist_ptl_const
hyperparameters:
  global_batch_size: 64
  learning_rate: 0.001
searcher:
  name: single
  metric: val_loss
  max_length:
      batches: 937 #60,000 training images with batch size 64
  smaller_is_better: true
entrypoint: model_def:MNistTrial
environment:
    image: ptl-image
