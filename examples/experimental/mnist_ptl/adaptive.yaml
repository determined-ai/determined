description: mnist_ptl_adaptive
hyperparameters:
  global_batch_size: 64
  learning_rate:
    type: double
    minval: .0001
    maxval: .1

searcher:
  name: adaptive_asha
  metric: val_loss
  smaller_is_better: true
  max_trials: 16
  max_length:
    batches: 937 #60,000 training images with batch size 64
entrypoint: model_def:MNistTrial
environment:
    image: ptl-image
