name: test_mmdetection
entrypoint: >-
   python3 -m determined.launch.torch_distributed
   python3 test_mm.py

# NEW: configure multiple slots per trial.
resources:
  slots_per_trial: 2

searcher:
   name: single
   metric: x
   max_length: 100

max_restarts: 0
environment:
  image:
    gpu: determinedai/model-hub-mmdetection:0.20.0