# pytest 6.0 has linter-breaking changes
pytest>=6.0.1
mypy==0.910
# responses 0.23.2 requires urllib3>=2, which breaks several other dependencies
responses!=0.23.2
requests_mock
coverage
deepspeed==0.8.3
transformers>=4.8.2,<4.29.0
attrdict3
moto
# Pydantic V2 has changes that break existing tests
pydantic<2
# setuptools needs to be pinned until we bump to torch>=1.11 and remove references to distutils.version
setuptools==65.5.1
