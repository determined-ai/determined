name: "Lint migrations"

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  lint-migrations:
    steps:
      - name: Run migration lint
        run: ./.github/scripts/lint-migrations.sh
        shell: bash
