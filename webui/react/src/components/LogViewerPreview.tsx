import React from 'react';

import css from './LogViewerPreview.module.scss';

const LogViewerPreview: React.FC = () => {
  return (
    <div className={css.base}>
      LogViewerPreview
    </div>
  );
};

export default LogViewerPreview;
