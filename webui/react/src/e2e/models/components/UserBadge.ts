import { Nameplate } from 'e2e/models/hew/Nameplate';

/**
 * Returns a representation of the error util.
 * This constructor represents the contents in src/components/UserBadge.tsx.
 * @param {object} obj
 * @param {CanBeParent} obj.parent - The parent used to locate this UserBadge
 * @param {string} [obj.selector] - Used instead of `defaultSelector`
 */
export const UserBadge = Nameplate;
export type UserBadge = Nameplate;
