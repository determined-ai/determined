name: core-api-searcher
entrypoint: python3 searcher.py --searcher asha --num-rungs 3 --max-trials 16 --divisor 4 --max-length 3000
searcher:
  metric: validation_error
  smaller_is_better: true
  name: single
  max_length:
    batches: 100
max_restarts: 0