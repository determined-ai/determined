name: core-api-searcher
entrypoint: python3 searcher.py --searcher random --max-trials 5 --max-concurrent-trials 2 --max-length 500
searcher:
  metric: validation_error
  smaller_is_better: true
  name: single
  max_length:
    batches: 100
max_restarts: 0