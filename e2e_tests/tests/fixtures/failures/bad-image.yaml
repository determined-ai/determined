description: An experiment with a bad docker image file that will error
environment:
  image: docker.io/badhost/missing.image
searcher:
  name: single
  metric: error
  max_length:
    batches: 1000
max_restarts: 0
entrypoint: failures:FailureTrial