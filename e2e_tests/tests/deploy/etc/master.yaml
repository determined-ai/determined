checkpoint_storage:
  type: shared_fs
  host_path: /tmp
  storage_path: ckpt-test/
  save_experiment_best: 0
  save_trial_best: 1
  save_trial_latest: 1

db:
  user: postgres
  host: determined-db
  port: 5432
  name: determined
  # password: database_password

launch_error: false
